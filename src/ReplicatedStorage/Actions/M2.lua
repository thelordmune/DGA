-- local StateManager = require(game.ReplicatedStorage.StateManager)
return function(character: Model, data: SkillData, ActionData)
	-- print(`{character.Name} is now doing the {data.skill} skill || timestamp: {data.timestamp}`) do

		-- StateManager.AddStateValue (character, "Attacking",data.skill, ActionData.Windup)

	end
end